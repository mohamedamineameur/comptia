{
  "name": "comptia-learning-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Comptia Security+ learning app monorepo (api + web + packages)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @comptia/api & npm run dev -w @comptia/web",
    "dev:api": "npm run dev -w @comptia/api",
    "dev:web": "npm run dev -w @comptia/web",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1"
  }
}
